# RPC should be the same as the one you are using in your local blockchain ( for this project we are using localhost as we only want to show simulation)
RPC_URL=http://localhost:8545
CONTRACT_ADDRESS=your_contract_address # It will be located in shared folder once you deploy the hardhat project in localhost : Refer "blockchain/scripts/deploy.ts" it contains that code